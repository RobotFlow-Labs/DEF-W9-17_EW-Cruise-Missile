FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml /app/
COPY src /app/src
COPY configs /app/configs

RUN pip install --no-cache-dir .
EXPOSE 8080
CMD ["uvicorn", "anima_ew_cruise_missile.serve:app", "--host", "0.0.0.0", "--port", "8080"]
